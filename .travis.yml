before_install:
  - sudo apt-get install -qq libboost-all-dev libjpeg-dev cmake clang
language: cpp
compiler:
  - clang

install:
  export CXX="clang++"
  export CC="clang"

script:
  - cmake ./
  - make && make test
